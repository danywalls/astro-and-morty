---
import { Image } from '@astrojs/image/components';
const { image , name, id} = Astro.props;
---
<div>
    <a href={`/character/${id}`}>
    <Image src={image} alt={name} format="webp" width={300} height={300} />
    </a>
</div>